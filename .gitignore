# Secrets — never commit
.env
.env.local
.env.*.local
ingestor/.env
web/.env.local

# Keep examples trackable
!.env.example
!**/.env.example

# Dependencies & build
node_modules/
.next/
out/
*.tsbuildinfo

# Go
ingestor/ingestor

# Python ML artifacts
ml-engine/data/
ml-engine/models/
ml-engine/venv/
ml-engine/.venv/
ml-engine/__pycache__/
ml-engine/**/__pycache__/

# OS
.DS_Store

# Nerd stat per-game caches (aggregate script output, not needed in deploy)
web/data/nerd-stats/**/games/
# Local game_state archive for zero-egress counter backfills (never commit)
web/data/nerd-stats-local/
