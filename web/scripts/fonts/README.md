# Brand font source

`IBMPlexSerif-Bold.otf` is IBM Plex Serif Bold (SIL Open Font License 1.1),
used only by `generate-wtbb-monogram.mjs` to outline the BB monogram.

Regenerate assets:

```bash
npm run brand:monogram
```
